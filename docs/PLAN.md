# PLAN — 이어서 할 일

기준일: 2026-09-21  
완료된 큰 줄기는 `docs/HANDOFF.md`.

## 원칙 (모든 작업)

- 최소수정. 다른 기능은 그대로
- UI 문자열·커밋 메시지는 한국어
- 독립 UI. 타 런처 복제 없음
- 저작권·특허 이슈를 피하려면 로고 복사·게시판 스크랩·시스템 자동연동을 넣지 않음
- 새 기능은 `docs/IP_DESIGN_LOG.md` 항목 추가
- 설치본이 받아야 하는 변경이면 버전 `0.1.x-YYYYMMDD` 올리고 `.\tauri-env.cmd build` 후 GitHub Releases에 `.exe` 업로드

## 2026-09-19에 넣은 것

- 사이트 칸 「인터넷 즐겨찾기」: 이 PC Edge·Chrome `Bookmarks`와 Windows `Favorites`의 `.url`
- 네트워크·CCTV 검색 시작부터 예상·경과·남은 시간, 검색 중 중지
- 업무도구 칸 「컴퓨터도구」: 이 PC `System32`의 고정 파일만 열어 이름 바꾸기·제어판·소리·인터넷 옵션·작업 관리자·시스템 정보 등 Windows 설정 화면을 연다. 익스플로러 설정 복원은 고정된 확인 화면만 연다. 이 PC IP 주소에서 사설·공인 IP만 본다. 홈 칸을 늘리지 않음
- 바로가기 카드 별로 홈 표시를 바꾸고, 빼기는 확인 후 목록에서만 뺀다. 관리자가 필요한 프로그램은 셸로 연다
- 보기 스킨 3개(서류·밝은 화면·어두운 화면). 홈 구역 제목을 진하게, 구역은 연한 배경으로 구분
- 홈 사이트는 한 줄 목록. 모두 목록 열은 설정에서 1열·2열
- 홈 자주 사용하는 도구의 종류 이름만 13px·본문색. 모두·즐겨찾기 단추 글자는 11px 유지
- 공지·사이트 Pack(`edulauncher-share`): 공지+사이트만 한 파일. 저장·가져오기 모두 공지와 사이트를 고름. 백업과 구분
- 패널 가로 약 560px 이상이면 홈·모두(2열 설정) 목록이 3열

## 2026-09-20에 넣은 것

- 처음 설치 기본: 시작 시 자동 실행·시작 시 창 표시 켜짐. NSIS 설치 직후 앱 실행(`src-tauri/windows/hooks.nsh`). 저장된 설정은 덮지 않음
- 실행 중 메인 패널은 작업 표시줄에도 아이콘. 창을 닫으면 최소화해서 아이콘이 남고, 아이콘을 누르면 다시 열림. 업무 그림·메모 창은 표시줄에 없음
- 업무도구 「QR코드 넣기」: 이 PC 그림 오른쪽 아래에 http(s) 주소 QR코드를 넣고 PNG로 저장 (`qrcode` 0.14.1). UI 이름 QR코드 넣기. DENSO WAVE 상표 고지. 특허 비침해를 보장하지 않음
- 바로가기 말풍선: 설명, 없으면 이름. 주소는 넣지 않음
- 홈 검색과 업무도구 「업무자료」: 교육행정 관련 업무 검색. 로컬 `topics.json`. AI 없음. 화면 이름 「관련 업무」
- 홈 검색 순서: 관련 도구 → 최근 사용 → 이 PC 폴더 → 학교 → 관련 업무. 키보드 이동도 같음
- 처음 화면 단추, 법령·일부 시스템 공식 주소 열기, 처리 흐름 상자 표시. 원문 파일은 넣지 않음
- 최근 사용에 열어 본 관련 업무 포함. 설정 하단에 소개 사이트 주소
- 할 일: 오늘(날짜)·내일·모레에 넣기. 저장은 달력 날짜. 날짜 줄 오른쪽 캘린더로 구글·네이버 사이트만 열기
- 공지 한 칸. 기관/부서 표시. Pack 고르기 추가. 이 PC에서 넣기·수정·빼기
- 이 PC 폴더 이름 찾기: 홈 검색(관련 업무보다 위) 최대 5건 + 컴퓨터도구 전용 화면. 바탕화면·문서·다운로드 기본. 내용은 읽지 않음
- 업무자료 검색결과·업무지도. 마인드맵은 로컬 JSON 목록. Topic id 유지. NotebookLM 수집 없음
- 업무자료 매뉴얼 JSON(`src/data/manuals/`). EPKI 인증서 트리를 접이식 업무지도·검색·상세에 연결. 그래프 라이브러리 없음
- 홈·업무자료 검색의 관련 업무 구역에 업무 그림. 크게 보기는 런처에서 조금 더 넓게 펼치고 그림을 창에 채움(`work-map`). 단추·휠로 크기, 끌어서 이동. 상자 누르면 패널 상세. 칸 크기는 스크롤바와 나누어 잰다. 자체 SVG. 새 패키지 없음. 별도 창은 패널과 같은 index.html을 열고 JSON 저장소는 열지 않음. 이미 있는 창은 지우고 다시 연다
- 패널 창 모서리로 크기 조절. `settings.json`에 너비·높이 저장. 처음 설치 440×650
- 폴더 바로가기는 `C:\Windows\explorer.exe`에 경로만 넘겨 연다. 파일·프로그램·사이트는 기존 방식
- 업무도구 「단축키」: 직장·교육기관용 단축키 20개. 실행은 고정 id만(탐색기·잠금·설정·작업 관리자·캡처). 키 입력 시뮬레이션 없음
- 홈 아래 메모 크게: 런처 옆 `memo-pad` 창. 제목 줄로 옮기고 모서리로 크기 조절. `settings.json`에 memoWidth/memoHeight 저장. 패널을 숨기면 같이 숨기고, 다시 열면 옮긴 자리·크기 유지. X로 닫으면 다음 크게는 다시 옆에서 염. plugin-store는 패널만 연다
- 업무 상세에서 뒤로 가면 직전 검색어와 그림·목록이 다시 나옴. 검색어는 화면 상태에만 둠. 상세 오른쪽 아래에 이전화면(처음 화면과 같은 모양)과 처음 화면이 있음
- 처음 설치에는 사이트·프로그램·파일·폴더가 비어 있음. 교육 바로가기 추가 단추 없음. 업무도구 기본은 QR코드 넣기. Network·CCTV는 담당자 Pack을 적용한 PC에만 목록에 생김. 컴퓨터도구·업무자료는 앱에 있음. 할 일 샘플도 넣지 않음. 홈 자주 사용하는 도구의 사이트·프로그램·파일·폴더는 비어 있으면 끌어놓기나 + 안내가 나옴. 종류마다 2열로 최대 6개까지 보이고, 더 있으면 모두에서 봄

## 이 PC 사용자 폴더 이름 찾기 (0.1.41)

홈 칸을 늘리지 않는다. 검색 결과는 주제·학교·도구·최근 사용 **다음**에만 붙인다.

- 홈: 두 글자 이상, 약 300ms 후, 최대 5줄. 기본은 바탕화면·문서·다운로드
- 더 보기 또는 컴퓨터도구 「이 PC 폴더 찾기」: 최대 40건. 사진·음악·동영상은 그 화면에서만 켬
- `USERPROFILE` 아래 해당 이름이 있는 폴더만. 심볼릭 링크 제외. 프로필 밖이면 중단. 깊이 3, 방문 8000, 검색어에 경로 문자 거부
- 파일 내용은 읽지 않음. 검색어·결과 목록 비저장. 클릭은 기존 `launch_tool`
- Everything·시작 메뉴·C:\ 전체 검색 아님. 새 npm/crate 없음

### 하지 말 것 (유지)

- `C:\` 전체, AppData, 다른 사용자 프로필, Windows·Program Files
- 파일 내용(한글·PDF 본문) 읽기, 검색어 저장, 인덱스 DB
- Everything·Windows 검색 UI 복제, 로고·설치파일 동봉
- 새 npm/crate. 파일을 런처 폴더로 복사

## 다음 후보 (우선 낮은 것부터)

1. **시작 시 검은 콘솔**  
   `.edupack` 연결용 `reg.exe`가 창을 띄움. `CREATE_NO_WINDOW`로 숨기기. Rust (`src-tauri/src/lib.rs`) + 설치본 재빌드.

2. **설치파일 자동 릴리스**  
   지금은 로컬 빌드 후 `gh release`로 수동 업로드. GitHub Actions(Windows runner + VS)는 선택.

3. **웹 미리보기**  
   실제 런처를 브라우저에 이식하지 않음. 목업 유지.

## 하지 말 것

- `src-tauri/target`, `node_modules`, `.exe`를 git에 넣기
- Cloudflare Pages에 설치파일 올리기
- Root directory 없이 저장소 루트를 웹으로 빌드하기 (런처 UI가 웹에 올라감)
- 루트에 `wrangler.jsonc`·`@cloudflare/vite-plugin`·`npm run deploy`를 두기 (앱 Vite와 사이트 배포가 섞임)
- `_redirects`의 `/* → /index.html` (Workers SPA와 루프)
- Chrome/Edge 북마크 JSON을 몰래 읽어 동기화하기
- APK·Play Store·Vercel 점수판 문구를 이 Windows 앱에 넣기
